import styled from '@emotion/styled';
import { Typography } from '@mui/material';

export const PokemonCount = styled(Typography)`
    align-self: flex-start;
    span {
        font-weight: 600;
    }
`;
